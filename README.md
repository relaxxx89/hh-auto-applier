# 🤖 HH.ru Auto Applier
<img width="1790" height="900" alt="image" src="https://github.com/user-attachments/assets/2d46f6df-4f02-48fa-a49b-040e5e357da6" />

Автоматический бот для отправки откликов на вакансии на [hh.ru](https://hh.ru).

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![Selenium](https://img.shields.io/badge/Selenium-4.0+-green.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

## ✨ Возможности

- 🔍 **Автоматический поиск** вакансий по заданным запросам
- 📝 **Выбор резюме** на основе типа вакансии (DevOps, SRE, Linux Admin)
- ✉️ **Сопроводительное письмо** — автоматическое добавление при необходимости
- 🚫 **Пропуск тестов** — вакансии с обязательными тестами пропускаются
- 💾 **Сохранение прогресса** — не откликается на одну вакансию дважды
- 🤖 **Автономный режим** — бесконечный цикл обработки всех вакансий без остановок
- 📄 **Многостраничный поиск** — автоматический просмотр всех страниц результатов (до 20 страниц)

## 📋 Требования

- Python 3.10+
- Google Chrome
- ChromeDriver (устанавливается автоматически через selenium)

## 🚀 Установка

### 1. Клонируй репозиторий

```bash
git clone https://github.com/relaxxx89/hh-auto-applier.git
cd hh-auto-applier
```

### 2. Создай виртуальное окружение

```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# Linux/MacOS
source .venv/bin/activate
```

### 3. Установи зависимости

```bash
pip install -r requirements.txt
```

### 4. Настрой конфигурацию

```bash
# Windows
copy config.example.yaml config.yaml

# Linux/MacOS
cp config.example.yaml config.yaml
```

Отредактируй `config.yaml` под свои нужды.

## ⚙️ Конфигурация

Файл `config.yaml`:

```yaml
# Поисковые запросы
search_queries:
  - "linux системный администратор"
  - "devops инженер"
  - "sre engineer"

# Ключевые слова для фильтрации (вакансия должна содержать хотя бы одно)
allowed_keywords:
  - "linux"
  - "devops"
  - "sre"
  - "системный администратор"

# Правила выбора резюме
resume_rules:
  - title: "DevOps-инженер"
    keywords: ["devops", "девопс"]
  - title: "SRE-инженер"
    keywords: ["sre", "site reliability"]
  - title: "Системный администратор Linux"
    keywords: ["linux", "системный администратор"]

# Сопроводительное письмо
cover_letter: |
  Здравствуйте!
  Заинтересовала ваша вакансия.
  Буду рад обсудить детали на собеседовании.
  Спасибо!

# Настройки поиска
area: 113  # 113 = Россия
schedule: "remote"  # remote, fullDay, shift, flexible, null (любой)
max_pages: 20  # Максимум страниц для обработки

# Chrome профиль (для сохранения сессии)
chrome_profile: "chrome_profile"
profile_name: "HH_Bot"

# Режим отладки (подробные логи)
debug: false
```

## 🎮 Использование

### Быстрый старт

```bash
# 1. Активируй виртуальное окружение
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/MacOS

# 2. Запусти бота
python run.py
```

### Базовый запуск

```bash
python run.py
```

### Дополнительные опции

```bash
# Использовать другой файл конфигурации
python run.py -c myconfig.yaml

# Включить режим отладки
python run.py --debug

# Показать версию
python run.py --version

# Показать справку
python run.py --help
```

### Первый запуск

1. Откроется браузер Chrome
2. Залогинься на hh.ru вручную
3. Дождись сообщения "Авторизация успешна!"
4. Бот автоматически начнёт работу

### Последующие запуски

Сессия сохраняется — авторизация не потребуется.

## 📊 Статусы вакансий

| Статус | Описание |
|--------|----------|
| ✅ `applied` | Отклик успешно отправлен |
| ⏭️ `already_applied` | Уже откликались ранее |
| 🚫 `mandatory_test` | Пропущена — обязательный тест |
| ❌ `not_suitable_keywords` | Не подходит по ключевым словам |
| 📝 `no_suitable_resume` | Нет подходящего резюме |
| ⚠️ `error` | Произошла ошибка при обработке |

## 💡 Советы по использованию

- **Используйте конкретные ключевые слова** — чем точнее фильтры, тем лучше результат
- **Настройте правила резюме** — бот автоматически выберет подходящее резюме
- **Проверяйте логи** — режим `--debug` покажет детальную информацию
- **Регулярно обновляйте** — следите за обновлениями проекта на GitHub

## 📁 Структура проекта

```
hh-auto-applier/
├── run.py               # 🚀 Точка входа (запуск бота)
├── config.yaml          # ⚙️ Конфигурация (создаётся пользователем)
├── config.example.yaml  # 📄 Пример конфигурации
├── requirements.txt     # 📦 Зависимости Python
├── README.md            # 📖 Документация
├── LICENSE              # 📜 Лицензия MIT
├── .gitignore           # 🚫 Git ignore
├── processed_vacancies.json  # 💾 Обработанные вакансии (создаётся автоматически)
├── skipped_vacancies.json    # 📋 Пропущенные вакансии (создаётся автоматически)
├── chrome_profile/      # 🌐 Профиль Chrome (создаётся автоматически)
└── src/                 # 📂 Исходный код
    ├── __init__.py      # 📦 Инициализация пакета
    ├── bot.py           # 🤖 Главный класс бота
    ├── config.py        # ⚙️ Загрузка конфигурации
    ├── storage.py       # 💾 Работа с JSON хранилищем
    ├── selenium_helper.py  # 🔧 Утилиты Selenium
    ├── vacancy.py       # 📄 Работа с карточками вакансий
    ├── modal.py         # 🪟 Модальное окно отклика
    └── logger.py        # 📝 Логирование с цветами
```

## 🔧 Возможные проблемы

### Ошибка "ModuleNotFoundError: No module named 'yaml'"

```bash
pip install pyyaml
```

### Браузер не запускается

Убедитесь, что установлен Google Chrome. Selenium автоматически скачает подходящий ChromeDriver.

### Бот не находит кнопку отклика

Возможно, hh.ru изменил структуру страницы. Откройте issue на GitHub.

## ⚠️ Важно

- **Не злоупотребляй** — hh.ru может заблокировать аккаунт за подозрительную активность
- **Используй разумные паузы** — бот уже имеет встроенные задержки
- **Проверяй конфигурацию** — убедись, что ключевые слова и резюме настроены правильно
- **Безопасность** — `config.yaml` и `chrome_profile/` не попадут в git благодаря `.gitignore`

## 🐛 Нашли баг?

Откройте [issue](https://github.com/relaxxx89/hh-auto-applier/issues) с описанием проблемы.

## 🤝 Contributing

Pull requests приветствуются! Для крупных изменений сначала откройте issue.

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

[MIT](LICENSE)

---

**⚠️ Disclaimer:** Этот проект создан исключительно в образовательных целях. Автор не несет ответственности за любое неправомерное использование. Используйте на свой страх и риск.

## 🙏 Благодарности

- [Selenium](https://selenium.dev/) — автоматизация браузера
- [hh.ru](https://hh.ru/) — крупнейший сайт по поиску работы в России
